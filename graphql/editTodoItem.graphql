mutation EditTodoItem($editedItem: TodoItemInput!) {
    editTodoItem(editedItem: $editedItem) {
        id
        title
        createdOn
        modifiedOn
        isCompleted
        completedOn
    }
}
